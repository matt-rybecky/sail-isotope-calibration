numpy>=1.21
pandas>=1.3
matplotlib>=3.5
scipy>=1.7
scikit-learn>=1.0
seaborn>=0.11
# Optional: only needed to read an ARM netCDF datastream in
# pub_arm_comparison.py. The archived ARM WVISO delivery is a single .dat
# (plain CSV) and does not require xarray.
xarray>=2022.3
